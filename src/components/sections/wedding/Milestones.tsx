import Milestones from "../../common/Milestones";
import { defaultMilestones } from "../../../data/milestones";

const WeddingMilestones = () => (
    <Milestones
        items={defaultMilestones}
        eyebrow="Milestones"
        title="Why Choose Us ?"
    />
);

export default WeddingMilestones;
